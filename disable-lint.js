const fs = require("fs"); const path = require("path"); const walkSync = (dir, filelist = []) => { fs.readdirSync(dir).forEach(file => { const dirFile = path.join(dir, file); if (fs.statSync(dirFile).isDirectory()) { filelist = walkSync(dirFile, filelist); } else { if (dirFile.endsWith(".tsx")) filelist.push(dirFile); } }); return filelist; }; const files = walkSync(path.join(__dirname, "src")); files.forEach(file => { let content = fs.readFileSync(file, "utf8"); if (!content.startsWith("/* eslint-disable */")) { fs.writeFileSync(file, "/* eslint-disable */\n" + content, "utf8"); console.log("Disabled lint in", file); } });
